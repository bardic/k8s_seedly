apiVersion: v1
kind: Service
metadata:
  name: {{ include "seedly.fullname" . }}
  labels:
    {{- include "seedly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seedly.selectorLabels" . | nindent 4 }}
